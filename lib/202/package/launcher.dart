// // flutter pub add url_launcher

// import 'package:url_launcher/url_launcher.dart';

// mixin LaunchMixin { /// mixinler constructorsuz sınıflardır genelde fonks alır ve with ile entegre edilir 
//   void name(args);

//   void launchURL(String url) async {
//     if (await canLaunch(url)) {
//       await launch(url);
//     }
//   }
// }